This Project Folder Contains 2 Project Folder,

Generate Code Folder Contains the Python Project :

It will generate the Parquet File , Custom Test Parquet, Multiple Customized Parquet file creation using Python.

2 project contains the Project with asp.net core api  which will read the Parquet file for test statistics and then done the calculation for final response.

I have created both cases , either create Parquet File , or we can see the json format result of the calculation on the basis of the files.

This below was the assignment tasks:


Create a service with one endpoint that:

1. accepts HTTP requests
2. expects parquet data to be contained in these requests
3. performs calculations on the parquet data in these requests
4. returns the results of the calculations as a new parquet file
